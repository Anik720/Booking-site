/** @type {import('next').NextConfig} */
const nextConfig = { images: {
    domains: [
      "i.pinimg.com",
      "assets.vogue.com",
      "m.media-amazon.com",
      "upload.wikimedia.org",
      "images.unsplash.com",
    ],
  },};

export default nextConfig;
